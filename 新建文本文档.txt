github结构是DAG

删除文件？
revert？
先添加、再删除的文件一直显示在change中？
切换分支：自动保存？
sync：sync是针对一个branch而言的，本地最新版本A与云端最新版本B会进行对比，并创建一个新的版本C：A有B没有或B有A没有的文件会被添加到C中，若A、B共有的文件有所不同，系统会要求处理冲突，处理后commit到本地（这个版本是兼容版本），再进行一次sync

